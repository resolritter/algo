const { run, print } = require("./utils")

const solve = (input) => {
  return input
}

run(
  __filename,
  solve,
  //
  [
    //
    [1, 1],
    [1, 1],
    //
  ],
  undefined,
  //
  { paired: true },
)
