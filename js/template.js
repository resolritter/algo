const { run, print } = require("./utils")

const solve = (as) => {
  return as
}

run(
  __filename,
  solve,
  // prettier-ignore
  [
    [1, 1],
    [1, 1],
  ],
  // prettier-ignore
  undefined,
  { paired: true },
)
